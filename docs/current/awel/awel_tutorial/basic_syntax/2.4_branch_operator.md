# 2.4 Branch 算子

`BranchOperator` 用于根据输入数据决定执行哪条路径。
例如，如果你有两条路径，你可以根据输入数据决定执行哪条路径。

有两种使用 `BranchOperator` 的方式：

## 使用分支映射构建 `BranchOperator`

向 `BranchOperator` 构造函数传递一个包含分支函数和 task 名称的字典。

```python
from dbgpt.core.awel import DAG, BranchOperator, MapOperator

def branch_even(x: int) -> bool:
    return x % 2 == 0

def branch_odd(x: int) -> bool:
    return not branch_even(x)

branch_mapping = {
    branch_even: "even_task",
    branch_odd: "odd_task"
}

with DAG("awel_branch_operator") as dag:
    task = BranchOperator(branches=branch_mapping)
    even_task = MapOperator(
        task_name="even_task", 
        map_function=lambda x: print(f"{x} 是偶数")
    )
    odd_task = MapOperator(
        task_name="odd_task", 
        map_function=lambda x: print(f"{x} 是奇数")
    )
```

在上面的示例中，`BranchOperator` 有两个子 task，`even_task` 和 `odd_task`。
`BranchOperator` 将根据输入数据决定执行哪个子 task。
因此，我们向 `BranchOperator` 构造函数传递一个包含分支函数和 task 名称的字典来定义分支映射。在字典中，键是分支函数，
值是 task 名称。当运行分支 task 时，所有分支函数都将被执行，
如果分支函数返回 `True`，则执行对应的 task，否则将被跳过。

## 实现自定义 `BranchOperator`

只需重写 `branches` 方法，返回一个包含分支函数和 task 名称的字典。

```python
from dbgpt.core.awel import DAG, BranchOperator, MapOperator

def branch_even(x: int) -> bool:
    return x % 2 == 0

def branch_odd(x: int) -> bool:
    return not branch_even(x)

class MyBranchOperator(BranchOperator[int]):
    def __init__(self, even_task_name: str, odd_task_name: str, **kwargs):
        self.even_task_name = even_task_name
        self.odd_task_name = odd_task_name
        super().__init__(**kwargs)
        
    async def branches(self):
        return {
            branch_even: self.even_task_name,
            branch_odd: self.odd_task_name
        }

with DAG("awel_branch_operator") as dag:
    task = MyBranchOperator(even_task_name="even_task", odd_task_name="odd_task")
    even_task = MapOperator(
        task_name="even_task", 
        map_function=lambda x: print(f"{x} 是偶数")
    )
    odd_task = MapOperator(
        task_name="odd_task", 
        map_function=lambda x: print(f"{x} 是奇数")
    )
```

## 示例

### 偶数或奇数

在 `awel_tutorial` 目录下创建一个名为 `branch_operator_even_or_odd.py` 的新文件，并添加以下代码：

```python
import asyncio
from dbgpt.core.awel import (
    DAG, BranchOperator, MapOperator, JoinOperator, 
    InputOperator, SimpleCallDataInputSource,
    is_empty_data
)

def branch_even(x: int) -> bool:
    return x % 2 == 0

def branch_odd(x: int) -> bool:
    return not branch_even(x)

branch_mapping = {
    branch_even: "even_task",
    branch_odd: "odd_task"
}

def even_func(x: int) -> int:
    print(f"分支 偶数，{x} 是偶数，乘以 10")
    return x * 10

def odd_func(x: int) -> int:
    print(f"分支 奇数，{x} 是奇数，乘以自身")
    return x * x

def combine_function(x: int, y: int) -> int:
    print(f"收到 {x} 和 {y}")
    # 返回第一个非空数据
    return x if not is_empty_data(x) else y
    
with DAG("awel_branch_operator") as dag:
    input_task = InputOperator(input_source=SimpleCallDataInputSource())
    task = BranchOperator(branches=branch_mapping)
    even_task = MapOperator(task_name="even_task", map_function=even_func)
    odd_task = MapOperator(task_name="odd_task", map_function=odd_func)
    join_task = JoinOperator(combine_function=combine_function, can_skip_in_branch=False)
    input_task >> task >> even_task >> join_task
    input_task >> task >> odd_task >> join_task

print("第一次调用，输入为 5")
assert asyncio.run(join_task.call(call_data=5)) == 25
print("=" * 80)
print("第二次调用，输入为 6")
assert asyncio.run(join_task.call(call_data=6)) == 60
```
注意：`can_skip_in_branch` 用于控制当前 task 是否可以在分支中被跳过。
将其设置为 `False` 以防止该 task 被跳过。

然后运行以下命令来执行代码：

```bash
poetry run python awel_tutorial/branch_operator_even_or_odd.py
```

你将在控制台中看到以下输出：

```bash
第一次调用，输入为 5
分支 奇数，5 是奇数，乘以自身
收到 EmptyData(SKIP_DATA) 和 25
================================================================================
第二次调用，输入为 6
分支 偶数，6 是偶数，乘以 10
收到 60 和 EmptyData(SKIP_DATA)
```

DAG 的图如下所示：

<p align="left">
  <img src={'/img/awel/awel_tutorial/branch_operator_example_1.png'} width="1000px"/>
</p>

在上面的示例中，`BranchOperator` 有两个子 task，`even_task` 和 `odd_task`，
它将根据输入数据和分支映射决定执行哪个子 task。

我们还使用了 `JoinOperator` 来合并来自两个子 task 的数据，如果某个路径被跳过，
`JoinOperator` 将收到 `EmptyData(SKIP_DATA)` 作为输入数据，我们可以使用
`dbgpt.core.awel.is_empty_data` 来检查数据是否为空数据。
