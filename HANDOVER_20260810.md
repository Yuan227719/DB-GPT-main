# DB-GPT react-agent 调试交接文档

> 日期：2026-08-10
> 项目：DB-GPT monorepo，`/v1/chat/react-agent`，半导体测试数据分析（st_embed 库、FL412E 项目、工单 SHCS26074748）

---

## 一、本次会话已修复的问题（全部已生效）

### 后端（已重启加载）

| # | 修复 | 文件 | 根因 |
|---|------|------|------|
| 1 | **解析器支持 `**Action**:` 格式** | `packages/dbgpt-core/src/dbgpt/agent/util/react_parser.py` | DeepSeek 输出 `**Action**:`（闭合星号在冒号前），旧归一化正则只认 `**Action:**`（冒号在星号内）→ 解析 0 steps → act() 守卫误判"没有工具调用"→ **第一步就早退** |
| 2 | **act() 兜底重写（去补丁）** | `packages/dbgpt-core/src/dbgpt/agent/expand/react_agent.py` | 删掉所有"Action Intention 检测/提及工具检测/计划语句检测"补丁，改为：**无 `Action:` 的纯文本一律要求 ReAct 格式**（要么给工具调用，要么 `Action: terminate`），不再盲目当最终答案接受 → 修"思考/计划文本被当最终答案"的早退 |
| 3 | **run_agent() 负责持久化** | `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/agentic_data_api.py` | 保存 view + 清 `_LIVE_AGENT_STEPS` 从生成器移到后台任务 `run_agent()` → 客户端断开（SSE 生成器被 GeneratorExit 取消）时结果仍会保存，F5 能看到 |
| 4 | **`_ACTIVE_AGENT_TASKS` 任务引用** | 同上 | 保持 `agent_task` 引用，防止生成器关闭时任务被 GC 取消导致持久化不执行 |
| 5 | **`updated_at` 实时刷新** | 同上（`_append_or_replace_step`） | `_LIVE_AGENT_STEPS` 的 `updated_at` 只在创建时设一次 → agent 跑 >15 分钟后 live 接口"僵尸兜底"误判 running=False → 前端 reload 看不到实时记录。现在每次 append step 都刷新 |
| 6 | **Kyuubi 连接重连** | `packages/dbgpt-ext/src/dbgpt_ext/datasource/rdbms/conn_kyuubi.py` | Kyuubi/Trino 空闲断开 Thrift 连接（TSocket read 0 bytes）后 SQLAlchemy 连接池一直复用死连接 → 所有 SQL 报 `unexpected exception`。加了 `pool_recycle=300` + `run()` 失败时 `engine.dispose()` 重试一次 |
| 7 | **get_table_schema OpenMetadata name-filter bug** | `packages/dbgpt-core/src/dbgpt/agent/util/openmetadata_client.py` | OpenMetadata 的 `/api/v1/tables` **不支持 `name` 过滤**（被忽略），代码传 `name=<table>&limit=20` → 只返回字母序前 20 张，靠后的表（如 `ods_mes_production_report` 排第 30）永远匹配不到 → 404。改为 `limit=200` 拉全量客户端匹配 |
| 8 | **get_table_schema 错误文本→Kyuubi 回退** | `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/agentic_data_api.py` | OpenMetadata MCP 把"表未找到"错误文本当成功返回 → Kyuubi 兜底被跳过。识别 "not found/Error executing tool" 等错误文本后回退 Kyuubi |
| 9 | **图表 JS 语法修复** | 同上（html_interpreter 内） | 模型用 code_interpreter 写 HTML 时 Python 把 JS 字符串字面量里的 `\n` 写成真实换行 → 浏览器 `SyntaxError: Invalid or unexpected token` → echarts 不渲染 → 图表区空白。渲染前对 `<script>` 块做真实换行→`\n` 转义修复 |

### 前端（18:02 已构建拷贝到 `packages/dbgpt-app/src/dbgpt_app/static/web`）

| # | 修复 | 文件 |
|---|------|------|
| 10 | **cleanFinalContent 剥 ReAct 前缀** | `web/pages/index.tsx` | 只剥 `Thought:/Action:` 没剥 `Action Intention:/Action Reason:` 也没处理 `**加粗**` → 模型原始 ReAct 文本泄漏到输出。现在兼容 `**Thought**:` 和普通格式 |
| 11 | **思考框：后台跑完自动消失** | 同上 | SSE 正常结束后清掉当前 view 的 `thinking` 标志（之前只清了错误路径） |
| 12 | **运行中禁止发送** | 同上 | `handleStart` 检查 `loading || convRunning` + 发送前查 live 兜底 |
| 13 | **F5 后思考框显示** | 同上 | `checkLive` 历史有旧 view 时也补建 live view；live view `thinking:true` |

### 之前已吸收的上游修复（git commit `2d3454c9`）
- output_parser 多 json fence、skill/loader SKILL.md、html_interpreter 一次性渲染提示

---

## 二、当前状态

- **后端运行中**（PID 需要在新会话确认），配置 `configs/openai.toml`
- **前端已构建**到 `packages/dbgpt-app/src/dbgpt_app/static/web`（18:02）
- 所有后端修复已重启加载
- git 未提交的部分改动在 worktree 里（注意 `git config core.fileMode false` 已设，忽略 WSL 权限噪音）

---

## 三、验证结果

### 通过的
- **Playwright 实时渲染**：`/tmp/mock_visible.js`（mock SSE → 可见步骤卡片 2 秒内 0→14）、`/tmp/precise_check.js`（已完成对话 13 个可见步骤卡片）、`/tmp/live_render_test.js`、`/tmp/mock_sse_test.js` 全 PASS
- **早退修复**：v_fix_0807 跑 8 步 0 失败（之前第一步就挂）；v_pw2_0807 跑 13 步
- **断开后保存**：`/tmp/disconnect_test2.sh` 验证客户端 10 秒断开后 agent 后台跑完，view 保存、live 清理
- **get_table_schema**：v_fix_0807 step-2 成功拿到 `ods_mes_production_report` 结构（38 列）
- **图表**：渲染的 HTML `node --check` 通过（不再 SyntaxError）

### 待确认/未解决
1. **DeepSeek 模型慢**：首个响应约 5 分钟，每个 LLM 调用 1-5 分钟。期间前端只显示"思考中"（这是正确的，但看起来像"卡住/滞后"）。**不是代码 bug，是模型速度**。如果用户觉得太慢，需换模型或调 DeepSeek 配置
2. **前端"刚开始看不到步骤"**：Playwright 验证前端渲染正常（可见卡片随 agent 推进出现），但用户坚持说"前端没有任何步骤"。可能原因：
   - 用户没刷新到最新前端（18:02）→ 需 **Ctrl+Shift+R 强制刷新**
   - 用户看的是 curl（scenario_test.sh）发起的会话，前端无 SSE 连接，需 F5/checkLive 轮询才会显示（我的 Playwright F5 测试显示正常）
   - **需要在新会话继续用 Playwright 精确复现用户的前端会话**（当前 `/tmp/cplx_live_check.js` 显示复杂对话 0-35s 只有 2 张卡片是因为 agent 首个 LLM 调用就要几分钟，后续才有更多步骤）
3. **模型偶发输出不规范 ReAct**：live 里偶现 step failed（如 v_pw2_0807 step-7/step-10），是模型输出质量问题，干净 act() 会提示重试并恢复，但会多耗轮次
4. **测试工具**：`/tmp/scenario_test.sh`（发问题+轮询完成）、`/tmp/eval_scenario.py`（评估质量）

---

## 四、如何重启 / 重建 / 测试

### 重启后端
```bash
cd /home/taoyuan/projects/DB-GPT-main
kill <旧PID>
nohup .venv/bin/dbgpt start webserver --config configs/openai.toml >> /tmp/dbgpt_server.log 2>&1 &
# 端口 5670
```

### 重建前端（改 index.tsx 后）
```bash
cd web
rm -rf .next out
npm run compile   # 约 7 分钟，OOM 风险（需 ~12GB 空闲）
rm -rf ../packages/dbgpt-app/src/dbgpt_app/static/web/*
cp -r out/* ../packages/dbgpt-app/src/dbgpt_app/static/web/
# 注意：改 index.tsx 必须 npx prettier --write pages/index.tsx 先过格式，否则构建报 prettier 错误
```

### 跑测试
```bash
/tmp/scenario_test.sh <conv_uid> "<问题>" <timeout秒>   # 发问题并等完成
python3 /tmp/eval_scenario.py <conv_uid>                # 评估质量
# Playwright:
NODE_PATH=/home/taoyuan/projects/DB-GPT-main/web/node_modules node /tmp/mock_sse_test.js
```

---

## 五、关键文件索引

- `packages/dbgpt-core/src/dbgpt/agent/util/react_parser.py` — ReAct 解析（`**Action**:` 归一化）
- `packages/dbgpt-core/src/dbgpt/agent/expand/react_agent.py` — act() 兜底（无 Action 要求 ReAct）
- `packages/dbgpt-core/src/dbgpt/agent/util/openmetadata_client.py` — OpenMetadata REST（limit=200）
- `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/agentic_data_api.py` — SSE 生成器 + run_agent 持久化 + live 更新 + get_table_schema 回退 + 图表修复
- `packages/dbgpt-ext/src/dbgpt_ext/datasource/rdbms/conn_kyuubi.py` — Kyuubi 重连
- `web/pages/index.tsx` — 前端所有修复

---

## 六、给新会话的下一步建议

1. 先确认后端/前端运行状态，`git status` 看未提交改动
2. **优先复现用户"前端看不到步骤"**：用 Playwright 通过真实前端发送复杂问题，精确数可见步骤卡片（参考 `/tmp/cplx_live_check.js`、`/tmp/frontend_send2.js`），确认是否真 bug 还是模型慢/旧构建
3. 若要继续完善：模型慢是最大体验瓶颈，考虑换模型或优化 DeepSeek 配置
